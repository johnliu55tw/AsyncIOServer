# Description

This is a simple python3 application that mimicking the dashboard, which basically is a server that opens up 2 ports `9001` and `9002` for regular and SSL socket connections from the Box, respectively.

## Dependencies

Since the application uses the [`selectors`](https://docs.python.org/3.5/library/selectors.html) module, python 3.4 or higher is required.
